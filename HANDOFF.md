# SF Move — handoff

## Status
v100 · Sep 10 · previous main head aaf8e17. Single-file PWA: index.html + sf-icon.png + data/lifetime-charges.json (source only — its 262 rows are INLINED as LIFETIME; the app never fetches at runtime) + three design/*mockup.html + design/verify/*.png (acceptance shots per build) + test/trip-time.test.js and test/trip-map.test.js (the repo's own assertions). All state in localStorage key sfMoveApp_v1 (+ sfMoveSync for the Gist ID/token/device, never merged, never in the repo). Served from epicminds-eng.github.io/sf-move (Pages deployment unverified this session).

## This session (v83 → v100)
- v100 chg-023 Buckeye, AZ (416 S Watson Rd, 07:16, 14.57 kWh, $5.39, 11 min) appended to CHARGES — 23 logged sessions, the first on Day 5. It reads as Arizona time via its address, draws a bolt node on the map and seeds one actual charging row onto Day 5 in Spend ($5.39 / 14.57 kWh, stopId "moms" because nearestStop is the leg's origin, as for every charge). NOTE: it sits 5.67 map units from chg-022 Chandler and CLUSTER_R is 6, so the two share ONE badged pin reading "2 stops here · 41.3 kWh · $19" — the existing clustering rule, though the two chargers are ~50 road miles apart. Tighten CLUSTER_R if that reads wrong on the phone.
- v99 THE SUB-TAB OWNS THE MAP FRAME, one rule and no exceptions. `frameMapFor(sec)` runs on every setTripSeg, on arriving at the Trip tab, and when Daily's day selector changes: Overview / Charging / Itinerary / Hotels fit the full route (literally `resetMapZoom`), Daily fits `legPts(day)` — the stop you left, the stop you reach and that day's chargers — and Places fits that day's places, falling back to every place when the day marked none, so it can never frame an empty stretch. `fitPts(pts,pad,maxS)` solves the scale from the visible box, pads by a fraction, floors the box at FIT_MIN=26 map units and caps the scale so a single place is centred rather than magnified; `clampZ` still keeps the map covering the box. `mapFrame()` reports the region actually on screen in map units — that is what "the frame" means in the tests. Layer toggles never re-frame (only the Lifetime crop changes VB, as before); manual pan and Reset still work and the next sub-tab switch overrides them. Tapping a hotel or place still drives the map, now parking the pin at PIN_FY=0.72 instead of 0.9 — v97's layer bar covers the bottom 48px of the map, so 0.9 was hiding the tapped pin underneath it.
- v99 MAP POPUPS are one shape: `popPill(href,name,meta)` — name on one line with an ellipsis, one short meta line, one chevron at the right edge, and the WHOLE pill is the Apple Maps link. Hotel "Tue Sept 8 · $231", place "4:55 PM · $17.83", charger "1:51 PM · 26.8 kWh · $13", lifetime "11 visits · 380.4 kWh · $30.91", cluster "2 stops here · 45.6 kWh · $19". The "Open in Apple Maps" line and the × are gone (a tap anywhere else on the map closes it). THE OVERLAP BUG WAS `.trmapc{line-height:0}` — set so the SVG has no descender gap — inheriting into the card and collapsing every line onto one baseline; `.pinpop` now sets its own `line-height:1.35`. The pill is 56px tall rather than the old collapsed 45px: that is what two legible lines actually need. Padding went to `var(--s2) var(--s3)` to hold it close.
- v99 WORDING, app-wide: no label announces that it is a link (rule now in CLAUDE.md) — "View charging details" → "Charging details", "tap to confirm the actual amount" → "an estimate, not confirmed yet". EVERY TILDE IS GONE from rendered text (27 sites: hours, miles, planned money, the Spend projection and its estimate paragraph, the Move/Sort reference prose). Highlights gained "Trip · every logged session" as its FIRST option and opens on it (ST_DEF.hlScope "day" → "all"). A place row title is one line with an ellipsis (`.row.one`), so "Rudy's Country Store & Bar-B-Q" no longer breaks as "Bar-B-/Q".
- v98 TIME ZONES: every STOPS row carries an IANA `tz`; `zonedToEpoch(wallClock,tz)` solves for the instant (Intl + formatToParts, one guess and one correction for DST). dayStats reads `rolled` in the PREVIOUS stop's zone and `arrivedAt` in that stop's, exposing rolledT/arrivedT. Day 3 door-to-door 565→685 min. Stamps and charge times are still STORED and DISPLAYED as wall clock: `chgTs` is the instant, `chgWall` is what every display site and every seeded Spend `ts` uses.
- v97 and before: the map layer bar, the 30%-taller hero (MAP_TALL), one-line stat rows, the Hotels section and map layer, the lifetime charging layer, the token system and one fluid layout.

## Rough
- Spend's per-day headers each print WHOLE dollars, so six rounded headers need not sum to the rounded grand total (chg-023 moved the fractions and the old verify-82 assertion drifted by $1). The invariant is per day: each header equals that day's own entries, rounded. Do not re-assert the sum.
- Places has no day picker of its own, so both Daily and Places frame `frameDay()` = Daily's selection if set, else today. Picking a day in Daily therefore also decides what Places frames.
- A place row's META line still wraps at phone width (only the title was made one-line — the brief said meta unchanged).
- Only STOPS and charge addresses carry zones; extend TZ_STATE if the route leaves IL/MO/OK/TX/KS/NM/CO/AZ/CA/NV.
- STAT MATH: chMin is ALL the day's charging minutes; chDrive is each session's OVERLAP with rolled → arrived, so a post-arrival top-up never shortens the drive.
- Everything is verified only in headless Chromium. Nothing has been run on a real iPhone: tab-bar detach, date picker, clipboard, share sheet, geolocation, the standalone PWA shell.
- The container's fallback font is WIDER than SF Pro, so the 320px fit margins in verify-97 are pessimistic.
- Map LOD collision uses estimated text boxes, not getBBox (the Trip page is display:none at init); a few LOD town labels sit outside the crop horizontally (pre-existing, reconciled rather than asserted away).
- A Reset or double-tap takes ~1.2 s to settle on 1×; until it lands the map keeps touch-action:none.

## Where things live
- Zones: TZ_STATE / zonedToEpoch / chgTz / chgTs / chgWall :2104–2140. Map frame: mapFrame / fitPts / FIT_MIN :2290, frameMapFor / legPts / placeDayPts :2862. Popups: hotelName / popPill / mapsUrl / pinPopOpen :2745–2810. PIN_FY :2695.
- Trip: renderTrip :1497, dayStats :2480, tripStats :2514, statrow :2905, renderPlacesSec :2428, renderChargingSec :2985, renderHotelsSec :3020. Other tabs: renderMove :1110, renderSort :1189, renderPack :1340, renderSpend :2626+, tabs :3066.
- TESTS in the repo: `NODE_PATH=$(npm root -g) node test/trip-time.test.js` (zones) and `test/trip-map.test.js` (frames, popups, the no-tilde and no-link-wording sweeps, Highlights, place rows). The verify-74…verify-97 + edge suites are still SCRATCHPAD-ONLY and die with the session. HARNESS RULE: never retype a number that also lives in index.html — reconcile from the source arrays, assert the delta a test causes, or assert structure. v99 repointed six suites that had frozen the old wording or the old popup text; v100 repointed two more — verify-80 now ASKS the app which day has no session instead of assuming Day 5, and verify-82 checks each day header against its own entries instead of summing rounded figures.

## Next
- Create the secret gist + a gist-scope classic PAT, connect the iPhone first (its data wins), then the iPad.
- Test on the actual iPhone/iPad as a standalone PWA, especially the map frame changes against real pinch/pan.
- Keep logging the drive: sessions into CHARGES (chg-023…), receipts into EXPENSES (exp-009…); both seed Spend once by id.
- Version numbering drifted three times earlier in the session; the footer is now the only source of truth and this build is v100. READ THE FOOTER before the next bump.
